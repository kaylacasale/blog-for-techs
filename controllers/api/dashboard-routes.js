const router = require('express').Router();
const { Comment, Blog, User } = require('../../models');
const withAuth = requi

//* put to update in api route
//* if press update, then comes to this route
