<div class="text-center">
    <h2>{{name}}</h2>
    <p>{{{get_emoji}}}</p>
    <p>{{description}}</p>
    <p>Created by {{user.username}} on {{format_date date_created}}</p>
    <p>
        <span for="img" aria-label="money">💲</span>
        <span class="dollar-amount">{{comment.comment}}</span>
        needed
    </p>
    <p>
        <button class="btn btn-primary">Submit!</button>
    </p>
</div>